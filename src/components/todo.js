import React from 'react';
import styles from '../styles/ToDo.css';

const ToDo = () => {
      <li id={data.id} className={styles.ToDo}>
        <h3>{data.id}</h3>
        <span>{data.text}</span>
      </li>
}

export default ToDo;
